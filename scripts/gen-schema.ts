/**
 * Rebuilds src/taxonomy/taxonomy.json from the fragments in data/, then
 * regenerates src/api/schema.ts with every component-ID enum drawn from the
 * flattened leaf IDs.
 *
 * Phase 0 stub. Exits 0 so the PostToolUse and Stop hooks that call it are
 * wired and non-fatal before phase 1 lands the real implementation.
 */

console.log('gen-schema: not implemented until phase 1.');
process.exit(0);
