/**
 * Enumerates every component ID referenced in src/, test/ and data/, and
 * asserts each exists in src/taxonomy/taxonomy.json. The closed-vocabulary
 * invariant, and the single most important test in the project.
 *
 * Phase 0 stub. Exits 0 so the hooks that call it are wired and non-fatal
 * before phase 1 lands the real implementation.
 */

console.log('validate-ids: not implemented until phase 1.');
process.exit(0);
