/**
 * Asserts every taxonomy leaf has a non-empty glosses.fr and a contrast_fr
 * whose status is one of transfer, near-miss, false-friend or novel, and that
 * no node carries a flat gloss string instead of the keyed map.
 *
 * Phase 0 stub. Exits 0 so the hooks that call it are wired and non-fatal
 * before phase 1 lands the real implementation.
 */

console.log('check-glosses: not implemented until phase 1.');
process.exit(0);
